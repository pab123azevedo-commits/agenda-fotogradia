[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Roda todo dia às 7h da manhã (horário de Brasília, UTC-3) — Netlify agenda em UTC
[functions."enviar-lembretes"]
  schedule = "0 10 * * *"
